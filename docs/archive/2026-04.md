# 2026-04 작업 아카이브

## 2026-04-16
- **프로젝트 초기화**: zm-codex 프로젝트 생성, Claude Code 셋팅 완료
  - CLAUDE.md, .claude/ 구조 (settings, rules 5, agents 5, hooks 6, memory 2, skills 3)
  - docs/ 관리 문서 구조 (project, session, features, archive)
  - 기술 스택 확정: FastAPI + Vite/React + PostgreSQL/pgvector
- **Phase 1 완료**: 양방향 연동 골격
  - DB 6 테이블 (projects, documents, hook_events, workflows, workflow_instances, step_executions)
  - Hook Receiver + SSE 브로드캐스터 + 프로젝트 등록 API (30개 문서 자동 스캔)
  - FE 실시간 이벤트 피드 (SSE) + settings.json HTTP 훅 추가
- **Phase 2 완료**: 워크플로우 관리자
  - 워크플로우 CRUD API + 인스턴스 관리 API
  - 개발 워크플로우 (7 nodes: 시작→설계→구현→타입체크→리뷰→문서갱신→완료)
  - 버그수정 워크플로우 (7 nodes: 시작→재현→원인분석→수정→검증→실수등록→완료)
  - FE @xyflow/react 노드 파이프라인 에디터 + 칸반 뷰 + 뷰 토글
  - 스킬/에이전트 매핑 (각 노드에 Claude Code 스킬/에이전트 연결)
- **Phase 3 완료**: 메모리 엔진
  - memory_chunks 테이블 + Vector(384) + HNSW cosine 인덱스
  - sentence-transformers all-MiniLM-L6-v2 임베딩 (로컬, API 불필요)
  - 문서 청킹 800자/100자 오버랩 + paragraph 경계 분할
  - 인제스트: 30 파일 → 45 청크 생성
  - 시맨틱 검색 API (벡터 코사인 유사도 정렬)
  - FE 검색 페이지 실제 API 연동 (Mock 교체)
- **Phase 4 완료**: 설정 동기화 + 변경 추적
  - 설정 비교 서비스 (.claude/ 디렉토리 양 프로젝트 비교)
  - 변경 추적 API (git log + 문서 변경 + 훅 이벤트 통합 타임라인)
  - FE Changes 페이지 실제 API 연동

## 2026-04-17
- **Phase 5~7 완료**: File Watcher · MCP Server · Drift Detection · 워크플로우 자동판단 · 설정 이력 · BM25 하이브리드 · 템플릿 생성 · Channel 서버 · 코드-문서 양방향 링크 (상세: docs/session/current-phase.md)
- **Phase 7 유지보수 — 워크플로우 관리자 전면 완성** (22 커밋, origin/main `e8c8632`..`150eea3`)
  - **BE 시드 API**: services/seed.py + 번들 4종(`bugfix/deployment/development/review`, review 7노드) + `GET /workflows/templates`, `POST /workflows/from-template`. NULL 바이트·path-traversal 가드, "updated" 경쟁 시 rollback+409
  - **FE UI 완성**: WorkflowCreateButton(템플릿 픽커·수동 입력·파일 가져오기) + WorkflowEditActions(Download/Pencil/Trash) + InstancePanel(생성·펼치기·status 4-way·current_node 선택·Radar 하이라이트·Trash 인라인 확인) + ModalShell·extractApiMessage 공용
  - **정합성**: 이름 중복 409(create+rename), delete 시 export `.md` 동반 제거, instances 누출 수정, button>div·controlled select 교정
  - **SSE 양방향**: `workflow_created/updated/deleted`, `instance_created/updated/deleted` 전부 broadcast + FE 구독
  - **인스턴스 DELETE**: `DELETE /{wf_id}/instances/{inst_id}` + 인라인 확인 UI + pytest
  - **MCP 변이 도구 2종 추가**: `create_workflow_from_template`, `create_instance` (update_step_status는 advance 겸임). MCP 도구 5 → 7
  - **리팩터**: `TemplateCreateRequest` → schemas/workflow.py, workflows.py E501 해소
  - **테스트**: pytest 20건 (templates·CRUD·중복·export/import·instance delete·MCP 왕복) + Playwright 3 스펙 (workflow-create/edit/instance)
  - **코드 품질**: Ruff 전 파일 49→0 청소 (자동 25 + 수동 23)
  - **가이드 문서 3종**: backend/tests/README.md, frontend/e2e/README.md, docs/features/workflow-manager.md
  - **실수 레지스트리**: M-004(button>div), M-005(select 불일치), M-006(pytest-asyncio loop), M-007(엔드포인트 검증 비대칭)
